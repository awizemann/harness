# macOS
.DS_Store
._*
.AppleDouble
.LSOverride

# Generated Xcode project — regenerate via `xcodegen generate`.
# The source of truth is project.yml.
Harness.xcodeproj/

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
*.xcscmblueprint
*.xccheckout

# Swift Package Manager
.build/
.swiftpm/
Packages/*/.build/
Package.resolved

# CocoaPods / Carthage (not used, but defensively ignored)
Pods/
Carthage/Build/

# Local Harness data (lives outside repo, but defensively ignored)
runs/
*.harness-run

# WebDriverAgent build artifacts (the submodule is checked in; build outputs aren't).
vendor/WebDriverAgent/build/

# Generated by the `Generate HarnessGeneratedRepoRoot.swift` pre-build script
# in project.yml. Bakes $SRCROOT into a constant so the running app can find
# vendor/WebDriverAgent. Per-machine; regenerated on every build.
Harness/Generated/

# Editor scratch
.vscode/
.idea/
*.swp
*.swo

# Local secrets / scratch
.env
.env.local
*.local.md
scratch/
TODO.local.md

# Private working files — kept locally for own use, never published.
# These were scrubbed from public git history (see CLAUDE.md / "Private working files").
/CLAUDE.md
/design-prompt.md
/docs/PRD.md
/docs/MANUAL_TEST_PLAN.md
/docs/DESIGN_BACKLOG.md
.claude/

# Public-surface worktrees — see CLAUDE.md / "Public surfaces".
.wiki-worktree/
.gh-pages-worktree/

---
title: HarnessMCP: Stdio MCP Server to Drive Harness from Agents
type: note
permalink: harness/tools/harnessmcp-stdio-mcp-server-to-drive-harness-from-agents
tags:
- mcp
- tooling
- cli
- architecture
---

## Observations
- [tool] HarnessMCP (new, v0.5): development-time stdio MCP server. New xcodegen `tool` target `harness-mcp`, built exactly like HarnessCLI — shares all of `Harness/` minus the SwiftUI/App surface. Entry point `HarnessMCP/Main.swift` pumps `NSApplication.run()` for WKWebView callbacks. #mcp #tooling
- [architecture] Hand-rolled JSON-RPC 2.0 over stdio (newline-delimited); zero new SPM deps. All logic inside one `MCPServer` actor; stdout = protocol, stderr = logs. Runs driven on detached tasks via `RunSupervisor` so the read loop never blocks. #architecture
- [persistence] Opens the GUI's on-disk SwiftData store via `RunHistoryStore.at(url:)` (`resetOnMigrationFailure:false`), so MCP-created data appears in the app and vice versa. #persistence
- [verified] Shared store CONFIRMED: app NOT sandboxed (`Harness.entitlements` app-sandbox=false) → GUI + harness-mcp both use `~/Library/Application Support/Harness/history.store`. Cross-process *fresh fetches* see each other's commits in real time (proven). The "no live refresh" caveat is only SwiftUI `@Query` auto-update. RunHistoryView reloads via `.task { reload() }` (manual fetch), so MCP runs DO appear in History on open/refresh. #persistence
- [seeding] harness-mcp seeds built-in personas on first tool call (idempotent, mirrors `AppContainer.bootstrapPersonas()`), guarded by `didSeedBuiltIns`. #seeding
- [tooling] 16 tools. v1.1 added `cancel_run` (manual abort) + an idle WATCHDOG in `RunSupervisor`: auto-cancels a run after `idle_timeout_seconds` with no `RunEvent` (default 180s, configurable on start_run, 0=off) — the backstop the step budget can't be (budget is only checked at the top of `runLeg`). Also `get_run_result` now reports live status for in-flight runs instead of the skeleton-zeros record. #tools
- [gotcha] start_run resolves persona_id -> `PersonaSnapshot.promptText` into `RunRequest.persona` (the `{{PERSONA}}` slot). It carries prompt TEXT, not a name. #gotcha
- [gotcha] Cloud runs read the API key via EnvKeychain: env var FIRST, then the GUI's Keychain item (owned by the GUI binary → a first read from harness-mcp can pop a macOS prompt; click Always Allow, or export the key in the client's env). #credentials
- [gotcha] While a run is in flight the persisted RunRecord is the skeleton (0 steps/tokens, no verdict) until `markCompleted`. `get_run_status` (in-process supervisor) is the real-time truth; `get_run_result` now detects in-flight and returns live status. #gotcha
- [finding] First real test (travelsynch.com, first-time-user persona, haiku, 2026-06-16): 12 clean steps in ~66s (landing → features → testimonials → pricing, 0 friction), then HUNG on a step-13 `tap_mark` of "Start Free Trial". Root cause: the web driver's post-action settle/load-wait has NO hard timeout, so a navigating click whose page never finishes loading freezes the run between `tool_result` and `step_completed`. Belongs in the web driver (`WebPlatformAdapter`/`WebDriver` settle/awaitNextLoad); affects GUI runs too. MCP watchdog + cancel_run are the backstops. Spawned a task to add the driver-level timeout. #finding
- [planned] App-side MCP-activity indicator approved (placement: BOTH global banner + sidebar 'Agent Sessions'; liveness: live step counter). Plan: harness-mcp writes an active-session marker file the GUI watches; same signal auto-refreshes RunHistory. Live screenshot mirror (tail events.jsonl + reuse RunReplay) is a follow-up. Needs design-tier consult before SwiftUI. #roadmap
- [build] `.mcp.json` server `harness`; binary `./.build/derived/Build/Products/Debug/harness-mcp` (gitignored). Build: `xcodegen generate` + `xcodebuild -scheme HarnessMCP -derivedDataPath ./.build/derived`. Smoke: `HarnessMCP/smoke-test.sh`. #build

## Relations
- relates_to [[HarnessCLI: Development-Time Driver — Shared Source, Same Artifacts]]
- relates_to [[Run Lifecycle & Orchestration]]
- relates_to [[Per-Application Credentials & Persona Library]]
- relates_to [[Platform Drivers — iOS, macOS, Web]]


## Audit findings & fixes (2026-06-16, multi-agent fresh-eyes review)
- [fix] The web-driver hang fix was INCOMPLETE at first — only `settle` was bounded. The audit caught that `probeInteractiveElements` (unbounded `evaluateJavaScript`) runs BEFORE the snapshot every step, and `dispatchClick`'s `runJS`/`runJSAndReturn` are unbounded too — so a navigating click to a hung page still froze the run one function later. Fix: EVERY per-step WKWebView await is now bounded via `WebDriver.raceAgainstTimeout` — settle, probe, runJS, runJSAndReturn, captureSnapshot. #fix
- [pattern] `raceAgainstTimeout` must use unstructured `Task`s + a continuation guarded by a one-shot actor (`RaceBox`). A `withTaskGroup` does NOT work here: it awaits ALL children before returning, so a wedged child re-introduces the hang. Abandon the wedged task; it's a bounded leak (≤1 main-actor-pinned call per timeout, freed at teardown). #pattern
- [fix] `captureSnapshot` now ferries a CGImage + the original POINT size across the race (NOT a TIFF round-trip, which can hand the image back at pixel size and misplace every Set-of-Mark badge). #fix
- [fix] Ghost RunRecord: `markCompleted` only runs on the happy path (`RunCoordinator.runAllLegs` end), so cancel/watchdog/error left a permanent "still running" skeleton in the shared store. Fixed MCP-side: `RunSupervisor` now holds `history` and writes a terminal record (`markCompleted` with `.blocked`/`.failure`) on cancel/fail/finishIfNeeded/idleCheck, plus an apply() guard to ignore late events after terminal. NOTE: a coordinator-level fix (terminal write in `RunCoordinator.execute`'s catch) would ALSO cover GUI-initiated aborts — still open. #fix
- [fix] Seeding flag (`didSeedBuiltIns`) now set AFTER a successful seed (was before the await), so a transient store failure retries instead of permanently serving empty personas. Safe: tool calls are serialized on the MCPServer actor + the seed is idempotent. #fix
- [open] Remaining audit findings NOT yet applied (recommended quick batch): (1) `MCPArguments.int()/bool()` coerce JSON booleans/numbers — e.g. `step_budget: true`→1, `step_budget: false`→0=unlimited, `include_archived: 5`→true — add `CFBooleanGetTypeID` guards; (2) `stage_credential` writes the SwiftData row BEFORE the Keychain password → orphan credential row on Keychain failure (write Keychain first, or compensating delete); (3) `start_run` doesn't validate `credential_id` belongs to `application_id` → cross-app password-injection risk (assert ownership); (4) `start_run` schema marks only `goal` required but the handler needs persona + platform params — clarify in the tool description; (5) no busy-retry on concurrent SwiftData `save()`; (6) supervisor `statuses`/`tasks`/`watchdogs` dicts never pruned; (7) GUI+MCP must ship lock-step SwiftData schema versions (two processes self-migrating one file is hazardous). #open


## Hardening batch applied (2026-06-16)
- [fix] Audit items 1–4 fixed + built + verified: (1) `MCPArguments.int()/bool()` reject mismatched JSON types via a CFBoolean guard (`isJSONBool`) — `step_budget: true` and `include_archived: 5` are no longer silently coerced; (2) `stage_credential` writes the Keychain password BEFORE the SwiftData row (no orphan row on Keychain failure); (3) `start_run` validates `credential_id` exists and — when `application_id` is set — belongs to it (blocks cross-app credential injection); (4) `start_run` tool description now states the goal + persona + target requirements. #fix
- [open] Still deferred (lower priority, mostly shared-code): busy-retry on concurrent SwiftData `save()` (touches shared RunHistoryStore, GUI too); supervisor `statuses/tasks/watchdogs` dict pruning (negligible leak); a coordinator-level terminal-record write in `RunCoordinator.execute`'s catch (would ALSO fix GUI-initiated abort ghosts — the MCP-side supervisor backstop already covers MCP aborts); GUI+MCP lock-step SwiftData schema versions (advisory). #open


## Validated end-to-end (2026-06-16)
- [verified] travelsynch.com retry on the rebuilt binary COMPLETED CLEANLY — 14 steps in 48s, verdict `blocked` = step-budget exhausted (NOT a hang). The agent drove INTO the email sign-in modal, typed an email, and clicked "Send sign-in link" → "Sending..." — exactly the dynamic-submit/navigation moment that froze run 1 (which hung on a "Start Free Trial" click). The web-driver per-step timeouts work. #verified
- [verified] Ghost-record fix confirmed in the shared store: the new run is a terminal RunRecord (completed, blocked, 15 steps); the pre-fix orphaned run (581CCC5F) still shows GHOST(running, 0 steps). New aborts won't ghost. #verified
- [process] To pick up a new harness-mcp binary in a live session, `pkill -x harness-mcp` — the MCP client auto-respawns the latest binary on the next tool call (verified). A running process can't hot-swap its binary. #process
- [note] Runs hitting the step budget end `blocked` mid-exploration with `wouldRealUserSucceed:false` — to get a real success/failure verdict via `mark_goal_done`, give a larger step_budget (~25–30). #note


## Agent-runs visibility feature (in progress, 2026-06-16)
- [goal] Show agent/MCP-driven runs in the GUI, distinct from user runs. Approved scope: origin marker + Agent Sessions sidebar section + live "session running" banner + cross-process refresh. Foundation (data model + cross-process bridge) DONE + validated; GUI (Phase 3) is next.
- [model] `RunOrigin` enum (gui/mcp/cli) — NOT `RunSource` (that name was already taken by GoalInputViewModel's payload-source picker; the MCP target excludes Features/ so the clash only showed in the app build). Stored on `RunRecord.sourceRaw` (schema V6), value derived from the run name (`mcp-run`/`cli-run`) at `skeleton(from:)` time. `RunRecordSnapshot.source` resolves stored value, falling back to name-derivation for legacy rows. #model
- [migration] V6 = lightweight add of `RunRecord.sourceRaw`. GOTCHA: each schema version must freeze ALL its models as DISTINCT nested classes (HarnessSchemaV5.RunRecord/Application/…). Sharing the live file-scope classes for the unchanged entities CRASHED at plan-validation with "Duplicate version checksums detected" (an NSException, not a catchable Swift error — would crash the app on launch). Fix: full V5 freeze, matching the V1–V4 pattern. Validated on the real store: 6 personas + 2 runs preserved, ZSOURCERAW column added cleanly. #migration #gotcha
- [safety] Mitigations used: backed up the store first; validated via the MCP opener (`resetOnMigrationFailure:false` → errors instead of wiping); and GRADUATED `RunHistoryStore.deleteStoreFiles` from delete to ARCHIVE-aside (moves to `.corrupt-<timestamp>`), so the GUI's `resetOnMigrationFailure:true` path can never silently destroy data again. #safety
- [bridge] `AgentSessionMarker` + `AgentSessionStore` (Harness/Core/Models.swift): the MCP `RunSupervisor` writes/updates `<appSupport>/agent-sessions/<runID>.json` per live run (currentStep, phase, goal, platform) and removes it on terminal — the cross-process signal the GUI will poll for the live banner + Agent Sessions list. #bridge
- [todo] Phase 3 GUI: `.agentSessions` sidebar section (AppCoordinator + SidebarView + DetailRouter), an @Observable monitor polling AgentSessionStore for the banner (StatusChip) + section, History: stop hiding agent runs under app-scope + a source badge, auto-reload on the marker signal. Components: StatusChip (banner), Pill/VerdictPill/SidebarRow (rows). #todo



## Phase 3 GUI shipped + validated (2026-06-16)
- [done] Agent-runs visibility GUI built on `harness-mcp-server`. All changes confined to the **app target** — `HarnessMCP`/`HarnessCLI` exclude `Features/**`, the `App/` shell, and `Mappers.swift`, and never include `HarnessDesign/`, so both tool schemes still BUILD SUCCEEDED untouched. #gui
- [finding] `HarnessDesign` is NOT a separate module — `project.yml` lists it as a `path:` under the Harness target's `sources:`, so it compiles INTO the app (no `public` needed; app types are in-scope). Convention still honored: kept it decoupled via plain strings (`Preview*` types), so SwiftUI previews work without the data layer. New `OriginBadge` primitive takes `label`+`systemImage` strings; the app's `Mappers.swift` maps `RunOrigin` → those strings. #gui #design
- [built] (1) `HarnessDesign/Primitives/OriginBadge.swift` — icon+label tinted capsule (Pill-styled). (2) `PreviewRun` gained `originLabel`/`originSystemImage` (nil default → memberwise init + mocks unaffected); `SidebarRow` renders `OriginBadge` next to the VerdictPill when set; `Mappers.PreviewRun(_ snapshot:)` fills them for non-`.gui` origins → History rows show an **"Agent"** badge. (3) `Features/AgentSessions/ViewModels/AgentSessionsMonitor.swift` — `@Observable @MainActor`, `Task.sleep(1.5s)` poll loop (codebase idiom, not Timer) over `AgentSessionStore.readAll()`; publishes `activeSessions` + an `endedGeneration` counter bumped when the set shrinks. (4) `AgentSessionsViewModel` (filters `fetchRecent` to `source == .mcp`) + `AgentSessionsView` (single-column dashboard: LIVE cards w/ StatusChip+live step counter, RECENT runs reusing SidebarRow, dbl-click→replay). (5) `AppContainer` owns `agentSessionsMonitor` and calls `start()` in init (idempotent). (6) `AppCoordinator` `.agentSessions` library section (sparkles) + `SidebarView.librarySections` + `HarnessApp` DetailRouter case. (7) `HarnessApp.RootView` `.overlay(alignment:.top)` → `AgentSessionBanner` (ultraThinMaterial capsule, StatusChip, "Agent session running — '<goal>' · step N", animates in/out on `sessions.isEmpty`). History auto-reloads via `.onChange(of: monitor.endedGeneration)`. #built
- [verified] Live e2e (travelsynch.com, haiku, step_budget 12, run 27B8F929): marker `<appSupport>/agent-sessions/<id>.json` written with `sourceRaw:"mcp"`, `phase:"running"`, `currentStep` advancing 1→6→7; removed on completion. Run finished `success` (7 steps) as a terminal RunRecord in the SHARED store; `ZSOURCERAW="mcp"` confirmed via sqlite (V6 column populated; legacy rows w/ empty sourceRaw still resolve `.mcp` via `name="mcp-run"` fallback). So: banner shows + live counter ticks + tears down; run lands in History with "Agent" badge and in Agent Sessions→Recent. GUI was live (PID running) during the test so its monitor refreshed History without re-nav. #verified
- [note] SourceKit threw ~60 "cannot find type" cross-file errors while editing (RunOrigin, HFont, Theme, RunRecordSnapshot, etc.) — all reindex noise before `xcodegen generate`; the real `xcodebuild` was clean. Trust the build, as the note warned. #gotcha
- [followup] Recommended a fresh-eyes multi-agent audit of the new GUI code (AgentSessions feature + banner + monitor + SidebarRow/PreviewRun/Mappers changes) before merge. Candidate nits to probe: live-card elapsed time is intentionally omitted (no per-second ticker — markers only repaint on step/phase change); banner animates on `sessions.isEmpty` only (step-count text updates aren't animated, by design); monitor poll diffs by `Array==` so identical reads don't churn. #followup



## Integration: thread agent runs into app History (2026-06-16, Alan's call)
- [problem] Fresh-eyes from Alan after Phase 3: the GUI felt like "two different apps." Root cause (confirmed by inspecting the live app + store): Harness is Application-centric — the whole **Workspace** tier (New Run / **History** / Friction) is gated on `selectedApplicationID != nil` (`SidebarView`). With ZERO Applications in the store and no `settings.json`, only the **Library** tier shows, so MCP runs (which were app-less) surfaced ONLY in the Agent Sessions island; the rich result surface (RunHistoryDetailView: summary/friction/path/replay) was unreachable. My Phase-3 "lands in History" was true at the data layer but the user literally couldn't navigate there. #problem
- [decision] Alan chose **"Thread agent runs into app History"** (over a global de-gated Runs timeline, or hardening the island): each agent run associates with an Application (match the target, else auto-create), so it flows into that app's normal History badged "Agent". Keeps the app-centric model. #decision
- [impl-mcp] `ToolHandlers.startRun`: ad-hoc runs (no `application_id`) now call new `resolveOrCreateApplication(_:_:)` → set `params.applicationID`. Match rule: web → existing `.web` app with same normalized host (`normalizedHost` strips scheme/`www.`), else create one named after the host (e.g. "travelsynch.com", webStartURL = target); iOS → match projectPath+scheme, else create named after scheme; macOS → match bundle path, else create named after the .app. Upsert happens BEFORE the run starts, so `RunHistoryStore.skeleton(from:)` (carries `request.applicationID`) resolves the relationship. App-less only remains when target params are insufficient (RunBuilder then throws its precise error). #impl
- [impl-gui] (1) `Mappers.PreviewRun(_:)` title fix — non-`.gui` runs show the GOAL, not the `mcp-run`/`cli-run` sentinel name (gate the name branch on `source == .gui`). (2) `RunHistoryViewModel.filteredRuns` — dropped the `source == .gui` app-scope exemption; ALL runs now scope by `applicationID` (the Agent badge distinguishes origin within an app). (3) `AgentSessionsView` recent rows now `openInHistory(_:)` — tap selects the run's Application (persists via AppState) + navigates to `.history` + selects the run, threading the island INTO the main flow; app-less legacy runs fall back to replay. #impl
- [verified] Rebuilt both schemes (BUILD SUCCEEDED). Quit old GUI, respawned harness-mcp, ran ad-hoc web (travelsynch.com, haiku, budget 10, run 3660B26B): store went 0→1 Applications ("travelsynch.com", web, https://travelsynch.com); the run's `ZAPPLICATIONLOOKUPID` = that app's id. Relaunched GUI → Applications shows "travelsynch.com" (with Recent runs in its detail) → Set as active unlocks Workspace tier → History shows the run badged **"✦ Agent"**, titled by its GOAL (not mcp-run), Success/8 steps, full detail pane (summary, API cost, path, replay/export). Verified visually via computer-use. The "two apps" feeling is resolved. #verified
- [note] Pre-change orphan agent runs (3, app-less) won't appear in any app's History now (scoped out); still reachable via Agent Sessions → replay. A one-time backfill (set their applicationID to the travelsynch.com app) would surface them — deferred unless Alan wants it. #note
- [followup] Combined change (Phase 3 GUI + this integration) still merits the fresh-eyes audit: probe the MCP match-or-create (host-normalization edge cases, concurrent app creation racing two ad-hoc runs for the same host → duplicate apps since match reads then writes without a uniqueness constraint), and the filter/title changes. #followup



## Fresh-eyes audit + fixes (2026-06-16, pre-release)
- [audit] 4 parallel independent reviewers (GUI/concurrency, MCP match-or-create, data-flow/regressions, design/HIG/a11y) over the Phase 3 + integration diff. Core verdict: sound; observation/main-actor isolation/animation/ordering/explicit-application_id path all verified correct. Build clean both schemes.
- [fix] Applied 7 fixes: (1) global banner `.allowsHitTesting(false)` — it's an overlay over the toolbar/sidebar and was intercepting clicks while a session was live (functional bug). (2) Ghost-marker staleness gate: `AgentSessionStore.readAll(now:)` now drops markers whose file mtime is older than `staleMarkerThreshold` (300s) — a `pkill harness-mcp` mid-run orphaned a marker that pinned a permanent fake "agent session running" banner (readAll is GUI-only; MCP only write/remove, so safe). (3) `AgentSessionsMonitor.poll` bumps `endedGeneration` on ANY run-id that disappeared (set-difference), not a net count drop — net-zero churn (one ends + one starts in the same 1.5s window) no longer skips the History reload. (4) `AgentSessionsView.openInHistory` guards that the target Application is live + unarchived before navigating (else falls back to replay) — navigating to an archived/deleted app stranded the user on a headerless workspace. (5) `resolveOrCreateApplication` de-`try?`s the `applications()` read so a store-read failure propagates instead of silently minting a duplicate app. (6) recent-runs list → `LazyVStack`. (7) `LiveSessionCard` `.accessibilityElement(children: .combine)`.
- [deferred] Conscious non-fixes (documented for a future pass): cross-process duplicate-Application race (two MCP processes, same new host, interleaved read→write — needs an `@Attribute(.unique)` business-key column on Application = a schema change, deferred to respect the V6 lock-step constraint); `http`/`https` host collapse (treated as same site — desirable grouping); app-less legacy/pre-association runs are now hidden in app-scoped History (no "All/Unassigned" scope — orphan backfill offered instead); native (iOS/macOS) target paths matched by exact string (web is normalized); `SidebarRow` reused across sidebar + detail pane (rename for clarity).
